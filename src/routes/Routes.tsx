import React from "react";
import {Switch, BrowserRouter} from "react-router-dom";

const Routes = () => {
    return (
        <BrowserRouter>
            <Switch>
            </Switch>
        </BrowserRouter>
    )

};

export default Routes;